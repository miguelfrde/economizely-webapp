import {combineReducers} from 'redux';

const initialReducer = (state = {}, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default combineReducers({
  initialReducer,
});
